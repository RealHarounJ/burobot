[build]
builder = "nixpacks"

[build.nixpacks]
aptPkgs = ["tesseract-ocr", "tesseract-ocr-ita", "libgl1-mesa-glx", "libglib2.0-0", "poppler-utils", "libmupdf-dev"]

[deploy]
startCommand = "uvicorn main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
